/**
 * Client-side product helpers have been removed in favor of server-side data fetching.
 * Use the utilities from `@/lib/data/products` or dedicated server actions instead.
 */

export { filterProducts, sortProducts } from '@/lib/data/products-shared';
